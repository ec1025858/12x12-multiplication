// Creating interger i to use with the for loop

for (int i = 1; i <= 12; i++)
{
    // Creating interger j
    int j = i;
// Making the table
    int result = i * j;
    Console.WriteLine($" | {i} |");
    
     Console.WriteLine("=========================");
    Console.Write($"{j} | {result} |");
    Console.WriteLine("=========================");
};